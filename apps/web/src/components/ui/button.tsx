// Re-export from the shared design system so the web app and the Word add-in
// consume one canonical primitive. Local imports of "@/components/ui/button"
// keep working unchanged.
export * from "@mike/shared/ui/button";
